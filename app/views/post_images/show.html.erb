<!--コントローラから渡された@post_image変数から、投稿画像やユーザ名などを表示しています。-->
<!--これは、一覧表示画面と同様の構造です。-->
<div class="post-body">
  <%= attachment_image_tag @post_image, :image %>
  <p>ショップ名：<%= @post_image.shop_name %></p>
  <p>説明：<%= @post_image.caption %></p>
  <p>ユーザーネーム：<%= @post_image.user.name %></p>
<!--'%Y/%m/%d'の記述で、年/月/日のフォーマットへ変換します。-->
<!--%Y、%m、%dは、日時のフォーマット指定によく使われます。-->
  <p>投稿日時：<%= @post_image.created_at.strftime('%Y/%m/%d') %></p>
<!--削除するためのリンク-->
<!--ifを用いてユーザを検証し、「投稿者」と「現在、ログインしているユーザ」が一致していなければ、削除ボタンが表示されないようにします。-->
  <% if @post_image.user == current_user %><!--もし、投稿者 == 現在ログインしているユーザ だったら-->
    <%= link_to "削除", post_image_path(@post_image), method: :delete %> <!--削除ボタン-->
  <% end %><!--終わり-->
</div>